#include "DBSynchroClientImpl.hpp"

/*
std::set<str::string> Contacts(const std::string& user_id, const size_t count) const override;

std::set<Event> Events(const std::string& user_id, const std::string& date) const override;

std::set<Groups> Groups(const std::string& user_id, const size_t count) const override;
 */
