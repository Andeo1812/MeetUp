#include <json.hpp>

#include <ParserRegAuth.hpp>

Context ParserRegAuth::StrToObject(const std::string& parser_str) const {
    Context res;

    return res;
}

std::string ParserRegAuth::ObjectToStr(const std::string type_response, const Context& other) const {
    std::string res;

    return res;
}

